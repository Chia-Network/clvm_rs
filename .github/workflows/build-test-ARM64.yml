name: Build ARM64 wheels

on:
  push:
    branches:
      - main
      - dev
      - arm64
    tags:
        - '**'
  pull_request:
    branches:
      - '**'

jobs:
  build_wheels:
    name: ARM64 Wheel on py-${{ matrix.python }}
    runs-on: ARM64
#    strategy:
#      matrix:
#        python: [37, 38, 39]
#        os: [ubuntu-latest]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
        fetch-depth: 1

    - name: Build Python 3.7 wheels
      run: |
        podman run --rm=true \
          -v ${{ github.workspace }}:/ws:rw --workdir=/ws \
          quay.io/pypa/manylinux2014_aarch64 \
          bash -exc '\
            curl -L https://sh.rustup.rs > rustup.rs && \
            chmod +x rustup.rs && \
            ./rustup.rs -y && \
            export PATH=$HOME/.cargo/bin:$PATH && \
            /opt/python/cp37-cp37m/bin/python -m venv venv && \
            if [ ! -f "activate" ]; then ln -s venv/bin/activate; fi && \
            . ./activate && \
            pip install maturin && \
            maturin build -i python --release --manylinux 2014 \
          '

    - name: Build Python 3.8 wheels
      run: |
        podman run --rm=true \
          -v ${{ github.workspace }}:/ws:rw --workdir=/ws \
          quay.io/pypa/manylinux2014_aarch64 \
          bash -exc '\
            curl -L https://sh.rustup.rs > rustup.rs && \
            chmod +x rustup.rs && \
            ./rustup.rs -y && \
            export PATH=$HOME/.cargo/bin:$PATH && \
            rm -rf venv && \
            /opt/python/cp38-cp38/bin/python -m venv venv && \
            if [ ! -f "activate" ]; then ln -s venv/bin/activate; fi && \
            . ./activate && \
            pip install maturin && \
            maturin build -i python --release --manylinux 2014 \
          '

    - name: Build Python 3.9 wheels
      run: |
        podman run --rm=true \
          -v ${{ github.workspace }}:/ws:rw --workdir=/ws \
          quay.io/pypa/manylinux2014_aarch64 \
          bash -exc '\
            curl -L https://sh.rustup.rs > rustup.rs && \
            chmod +x rustup.rs && \
            ./rustup.rs -y && \
            export PATH=$HOME/.cargo/bin:$PATH && \
            rm -rf venv && \
            /opt/python/cp39-cp39/bin/python -m venv venv && \
            if [ ! -f "activate" ]; then ln -s venv/bin/activate; fi && \
            . ./activate && \
            pip install maturin && \
            maturin build -i python --release --manylinux 2014 \
          '

    - name: Upload artifacts
      uses: actions/upload-artifact@v2
      with:
        name: wheels
        path: target/wheels/

    - name: Install Twine
      run: |
        python3 -m venv venv
        if [ ! -f "activate" ]; then ln -s venv/bin/activate; fi
        . ./activate
        pip install twine

    - name: Test for secrets access
      id: check_secrets
      shell: bash
      run: |
        unset HAS_SECRET
        if [ -n "$SECRET" ]; then HAS_SECRET='true' ; fi
        echo ::set-output name=HAS_SECRET::${HAS_SECRET}
      env:
        SECRET: "${{ secrets.test_pypi_password }}"

    - name: publish (PyPi)
      if: startsWith(github.event.ref, 'refs/tags') && steps.check_secrets.outputs.HAS_SECRET
      env:
        TWINE_USERNAME: __token__
        TWINE_NON_INTERACTIVE: 1
        TWINE_PASSWORD: ${{ secrets.pypi_password }}
      run: |
        . ./activate
        twine upload --non-interactive --skip-existing --verbose 'target/wheels/*'

    - name: publish (Test PyPi)
      if: steps.check_secrets.outputs.HAS_SECRET
      env:
        TWINE_REPOSITORY_URL: https://test.pypi.org/legacy/
        TWINE_USERNAME: __token__
        TWINE_NON_INTERACTIVE: 1
        TWINE_PASSWORD: ${{ secrets.test_pypi_password }}
      run: |
        . ./activate
        twine upload --non-interactive --skip-existing --verbose 'target/wheels/*'

    - name: Clean up
      run: |
        rm -rf venv
        rm -rf target
